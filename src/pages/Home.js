import React from "react";
import Layout from "../layouts/Layout";
import { Container } from "react-bootstrap";

const Home = () => {
  return (
    <Layout>
      <Container style={{ minHeight: "75vh" }}>Home</Container>
    </Layout>
  );
};

export default Home;
